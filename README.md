# flight-analyzer

## How to create a regression model

Run "spark-submit model/model.py" while in the project directory
You can use "--input" to specify the input data and "--output" for outputing the model and "--label" for labeling the column to predict
